/// <reference types="vite/client" />

interface ImportMetaEnv {
  readonly VITE_BACKEND_URL: string;
  readonly VITE_BACKEND_MUESTRAS_URL: string;
  // más variables de entorno pueden agregarse aquí
}
